{
  "name": "nest-config",
  "version": "1.0.0",
  "description": "Shared NestJS config and database modules",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "knex": "^3.1.0",
    "pg": "^8.12.0",
    "drizzle-orm": "^0.44.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}